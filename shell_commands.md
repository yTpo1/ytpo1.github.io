# Basic Shell Commands (Cerminal Commands)

Copy a file from one location to another:
```
cp source.txt destination.txt
```

Copy a directory from one location to another:
```
cp -r source destination
```