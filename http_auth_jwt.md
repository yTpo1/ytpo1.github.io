# HTTP Authentication & Authorization - JWT Token

[JWT (JSON Web Token)](./jwt.md)

The following HTTP message header is added to both `GET` and `POST` method requests.
```
Authorization: Bearer jwtAccessToken
```